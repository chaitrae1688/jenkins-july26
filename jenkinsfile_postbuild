pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Post Build'
            }
        }
    }
    post {
        always {
            build('Team A/Project A')
        }
    }
}
